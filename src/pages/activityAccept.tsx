import { RecoilRoot } from "recoil";
import ActivityAccept from "@/components/activityAccept/ActivityAccept";

export default function ActivityAcceptPage() {
  return (
    <RecoilRoot>
      <ActivityAccept />
    </RecoilRoot>
  );
}
